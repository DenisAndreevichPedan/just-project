# Week 2 — Promises, `async/await`, error propagation

**Среда:** Node v22  
**Формат:** 5 мини-лаб по 20–45 минут  
**Цель недели:** руками почувствовать, как работают цепочки `Promise`, паузы на `await`, распространение ошибок и базовые практические паттерны обработки async-кода.

---

## Как проходить неделю

1. Открой нужный файл дня.
2. До запуска выпиши прогноз порядка логов или результата каждого кейса.
3. Запусти файл через `node ...`.
4. Сравни прогноз с фактом и коротко запиши, где ошибся.
5. В конце дня сформулируй 1–2 правила своими словами.

---

## Day 1 — Promise order and microtasks

**Файл:** `labs/promises/week2-day1-promise-order.js`

**Зачем:** понять, почему `.then`, `.catch` и `.finally` иногда ведут себя "неинтуитивно", и перестать гадать при дебаге async-логики.

### Таблица наблюдений

| # | Код (кратко) | Мой прогноз | Факт | Совпало? |
|---|---|---|---|---|
| 1 | `Promise.resolve().then` + `sync` |  |  |  |
| 2 | два `.then` на одном resolved promise |  |  |  |
| 3 | `.then` внутри `.then` |  |  |  |
| 4 | `.catch` после `throw` в `.then` |  |  |  |
| 5 | `.finally` и значение цепочки |  |  |  |
| 6 | `setTimeout` vs promise chain |  |  |  |

1-ая неделя:
1) совпало, так как я понимаю, что сначала идет синхронный код, а затем тот, который отложился в микрозадачу через then
2) совпало, так как я понимаю, что сначала идет синхронный код, а затем по очереди тот, который отложился в очередь микрозадач  
3) совпало, так как я понимаю, что сначала добавляются в очередь первый then, далее идет синхронный код, на второй итерации: выполняется код, который идет в первом then, добавляется в очередь микрозадач код от then из внутреннего промиза, добавляется в очередь микрозадач второй наружний then из цепочки, на третей итерации: выполняется код из внутреннего then, затем выполняется код из второго внешго then из цепочки
4) совпало, так как сначала - синхронный, затем из очереди микрозадач первый лог, и так как промиз отвалился, в очередь микрозадач идет колбэк из кэтч и потом выполняется оттуда
5) сначала идет синхронный лог, затем коллбек из файнелли попадает в очередь микрозадач, выводится лог из него, что означает return: "ignored" - я не знаю, поэтому не знаю, будет ли выполняться then, но предположу все таки, что будет и его коллбек улетит в очередь микрозадач, после чего выполнится и выведет лог из себя
6) коллбек с сетТаймаут уходит в очередь макрозадач, коллбек из первого then от промиза уходит в очередь микрозадач, выполняется синхронный лог, выполняется колбэк из очереди микрозадач, колбек из второго then ставится в очередь микрозадач, так как первый then выполнился, выполняется лог из колбэка из очереди макрозадач, выполняется колбэк второго then из очереди микрозадач

2-ая неделя:
1) 
1-ая версия: сначала идет первый синхронный лог, потом идет выполнение функции noAwait, выводится первый в ней синхронный лог, затем колбэк из then улетает в очередь микрозадач, выполняется синхронный лог последний, затем выполняется микрозадача
2-ая версия: "[1] before", noAwait уходит в какую-то очередь, выводится "[1] after", затем выполняется noAwait, где выполняется первая по ходу лог, и потом уходит в очередь микрозадач колбэк из then и сразу выполняется

2) выводится [2] before call, затем [2] before await, весь код после евейта ставится в очередь микрозадач, выводится [2] after call, затем выполняется микрозадача и выводится [2] after await

3) сначала [3] before call, затем [3] step 1, затем в очередь микрозадач улетает все просто первого евейта резолва, затем [3] after call, затем выполняется микрозадача и выводится [3] step 2, затем после второго евейта резолва последний консоль лог улетает в очередь микрозадач, а затем выполняется как микрозадача, выводя [3] step 3

4) выводится [4] sync start, затем [4] A before await, работа по функции А откладывается в очередь микрозадач, выводится [4] B before await, работа по функции Б откладывается в очередь микрозада, выводится [4] sync end, выводится [4] A after await, [4] B after await

5) [5] before call, [5] before await 42, не знаю, что значит await 42, но предполагаю просто, что это число.

Т.е. в очередь микрозадач уходит хвост console.log("[5] after await:", value), выводится [5] after call, выводится [5] after await: 42




6) выводится [6] sequential start, ставится в микроочередь хвост после await sequential(). При выполнении sequential выводится [6] sequential, ставится в микроочередь оставшийся хвост после await wait(80, "A"), выполняется wait(80, "A"). 

Дальше для меня сложнее, так как я не шарю за такие промизовые конструкции и порядок выполнения кода при этом, но предположу:
далее возвращается объявление колбэка из промиза, но он не выполняется, так как я не вижу его вызова. Затем выполняется из микроочереди хвост case6. Выводится [6] parallel start, далее выводится [6] parallel, в микроочередь идет все после await Promise.all([wait(80, "A"), wait(80, "B")]). 
Одновременно выполняется Promise.all([wait(80, "A"), wait(80, "B")]), но как именно это все выполняется, я не знаю.
Предполагаю, что опять возвращается два объявленных колбэка от функции wait. Но опять ничего не выводится, так как только объявляется и нет вызова. 

Далее из микроочереди достается остаток от функции sequential, объявляется коллбэк от промиза в wait(80, "B"), в микроочередь уходит хвост: console.timeEnd("[6] sequential").

Далее из микроочереди выполняется console.timeEnd("[6] parallel"). (как выполняется я не знаю, не шарю за такой метод консоли). Далее выполняется console.timeEnd("[6] sequential"). (аналогично не знаю, как именно)






### Мини-отчёт

- Где ошибся:
- Какое правило подтвердилось:
- Где это встречается в реальной разработке:

---

## Day 2 — `async/await`: pause and resume

**Файл:** `labs/promises/week2-day2-async-await.js`

**Зачем:** перестать воспринимать `await` как "магическую синхронность" и начать видеть точки паузы, продолжения и реальные границы выполнения.

### Таблица наблюдений

| # | Код (кратко) | Мой прогноз | Факт | Совпало? |
|---|---|---|---|---|
| 1 | `async` без `await` |  |  |  |
| 2 | один `await Promise.resolve()` |  |  |  |
| 3 | два `await` подряд |  |  |  |
| 4 | две async-функции вперемешку |  |  |  |
| 5 | `await` обычного значения |  |  |  |
| 6 | sequential vs parallel |  |  |  |

### Мини-отчёт

- Что именно делает `await`:
- Почему `async` не делает код синхронным:
- Где это важно в React/Next:

---

## Day 3 — Errors: `throw`, `reject`, `try/catch`

**Файл:** `labs/promises/week2-day3-error-propagation.js`

**Зачем:** senior не просто "ловит ошибку", а понимает, где она превратится в rejected promise, где потеряется и где станет unhandled rejection.

### Таблица наблюдений

| # | Код (кратко) | Мой прогноз | Факт | Совпало? |
|---|---|---|---|---|
| 1 | `throw` внутри `.then` |  |  |  |
| 2 | `Promise.reject().catch` |  |  |  |
| 3 | `try/catch` без `await` |  |  |  |
| 4 | `try/catch` с `await` |  |  |  |
| 5 | `return Promise.reject()` из `async` |  |  |  |
| 6 | `finally` после ошибки |  |  |  |

### Мини-отчёт

- Какая ошибка оказалась неожиданной:
- Когда `try/catch` не помогает:
- Какое правило забираю в рабочий код:

---

## Day 4 — Promise combinators in practice

**Файл:** `labs/promises/week2-day4-promise-combinators.js`

**Зачем:** понять, когда нужен `Promise.all`, когда `allSettled`, чем опасен `race` и как это связано с реальными загрузками нескольких источников данных.

### Таблица наблюдений

| # | Код (кратко) | Мой прогноз | Факт | Совпало? |
|---|---|---|---|---|
| 1 | `Promise.all` — всё успешно |  |  |  |
| 2 | `Promise.all` — один падает |  |  |  |
| 3 | `Promise.allSettled` |  |  |  |
| 4 | `Promise.race` |  |  |  |
| 5 | `Promise.any` |  |  |  |
| 6 | sequential vs parallel time |  |  |  |

### Мини-отчёт

- Чем `all` отличается от `allSettled`:
- Где можно словить баг через `race`:
- Какой комбинатор выбрал бы для загрузки dashboard:

---

## Day 5 — Practical async patterns

**Файл:** `labs/promises/week2-day5-practical-patterns.js`

**Зачем:** перейти от "понимаю примеры" к "могу собрать безопасное поведение" для загрузок, ошибок, cleanup и timeout.

### Таблица наблюдений

| # | Код (кратко) | Мой прогноз | Факт | Совпало? |
|---|---|---|---|---|
| 1 | `try/catch/finally` и `return` |  |  |  |
| 2 | timeout wrapper |  |  |  |
| 3 | success/error result object |  |  |  |
| 4 | cleanup при ошибке |  |  |  |
| 5 | параллельные запросы с частичным фейлом |  |  |  |

### Мини-отчёт

- Какой паттерн хочется унести в проект:
- Где нужна явная модель результата вместо `throw`:
- Что бы ты добавил в `safeFetch`:

---

## Итог недели

В конце недели ответь письменно на 5 вопросов:

1. Чем отличается `throw` внутри `.then` от `throw` в обычной функции?
2. Почему `try/catch` не ловит promise-ошибку без `await`?
3. Что именно делает `await` с продолжением функции?
4. Когда `Promise.all` подходит, а когда ломает UX?
5. Какой минимальный паттерн "безопасного async-кода" ты теперь используешь по умолчанию?

---

## Мини-челлендж недели

За 20–40 минут, без AI:

1. Напиши `safeFetchLike(task)` так, чтобы она возвращала:
   - `{ ok: true, data }` при успехе
   - `{ ok: false, error }` при ошибке
2. Добавь timeout на 300ms.
3. Покрой 3 сценария логами или простыми тестами:
   - success
   - reject
   - timeout

---

## Команды запуска

```bash
node labs/promises/week2-day1-promise-order.js
node labs/promises/week2-day2-async-await.js
node labs/promises/week2-day3-error-propagation.js
node labs/promises/week2-day4-promise-combinators.js
node labs/promises/week2-day5-practical-patterns.js
```
